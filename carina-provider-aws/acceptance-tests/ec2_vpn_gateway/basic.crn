# EC2 VPN Gateway - Basic test
#
# Tests: VPN Gateway creation

provider aws {
  region = aws.Region.ap_northeast_1
}

let vgw = aws.ec2.vpn_gateway {
  type = aws.ec2.vpn_gateway.Type.ipsec.1

  tags = {
    Environment = "acceptance-test"
  }
}
