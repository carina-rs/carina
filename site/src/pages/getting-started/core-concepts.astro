---
import Doc from '../../layouts/Doc.astro';
import { Content, getHeadings, frontmatter } from '../../content/getting-started/core-concepts.md';

const headings = getHeadings();
---
<Doc
  title={frontmatter.title ?? 'Core Concepts'}
  description={frontmatter.description}
  headings={headings}
>
  <Content />
</Doc>
