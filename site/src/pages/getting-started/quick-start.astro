---
import Doc from '../../layouts/Doc.astro';
import { Content, getHeadings, frontmatter } from '../../content/getting-started/quick-start.mdx';

const headings = getHeadings();
---
<Doc
  title={frontmatter.title ?? 'Quick Start'}
  description={frontmatter.description}
  headings={headings}
>
  <Content />
</Doc>
