---
import Doc from '../../../layouts/Doc.astro';
import { Content, getHeadings, frontmatter } from '../../../content/reference/cli/providers.md';

const headings = getHeadings();
---
<Doc
  title={frontmatter.title ?? 'carina providers'}
  description={frontmatter.description}
  headings={headings}
>
  <Content />
</Doc>
