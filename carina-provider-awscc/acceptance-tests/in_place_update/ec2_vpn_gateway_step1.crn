# EC2 VPN Gateway - in-place update test (step 1)
#
# Tests: create VPN gateway with initial tags

provider awscc {
  region = awscc.Region.ap_northeast_1
}

awscc.ec2.vpn_gateway {
  type = "ipsec.1"

  tags = {
    Environment = "acceptance-test"
  }
}
