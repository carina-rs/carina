# EC2 Internet Gateway - Basic test
#
# Tests: tags

provider awscc {
  region = awscc.Region.ap_northeast_1
}

let igw = awscc.ec2_internet_gateway {
  tags = {
    Environment = "acceptance-test"
  }
}
