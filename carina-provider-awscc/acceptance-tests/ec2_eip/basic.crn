# EC2 EIP - Basic test
#
# Tests: domain, tags

provider awscc {
  region = awscc.Region.ap_northeast_1
}

let eip = awscc.ec2_eip {
  domain = "vpc"

  tags = {
    Environment = "acceptance-test"
  }
}
