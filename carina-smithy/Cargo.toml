[package]
name = "carina-smithy"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Smithy 2.0 JSON AST parser for Carina"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
