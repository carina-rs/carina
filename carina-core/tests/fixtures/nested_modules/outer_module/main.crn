let inner = use { source = '../inner_module' }

arguments {
  cidr_block: String
}

let net = inner {
  cidr_block = cidr_block
}

let sg = awscc.ec2.SecurityGroup {
  group_description = 'test'
  vpc_id = net.vpc_id
}
