[package]
name = "carina-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
pest = "2"
pest_derive = "2"
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
